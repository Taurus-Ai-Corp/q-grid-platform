'use client'

// PostHog analytics placeholder — install posthog-js when ready
// For now, just track page views via a simple beacon

export function Analytics() {
  // Placeholder — PostHog integration will be added when API key is provisioned
  return null
}
