# Dependencies
node_modules/

# Next.js
.next/

# Build outputs
dist/

# Turborepo
.turbo/

# Vercel
.vercel/

# Environment files
.env
.env.local
.env*.local

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/
.vercel
.DS_Store
.claude/settings.json

.verify-screens/

# Sync-tool conflict copies ("resolve 2.ts", "project 2.json"). Documents/ is a
# sync root, so these appear silently next to the real file. One landed in
# packages/db/src/ holding a stale copy of the residency-routing logic — tsc
# globs src/**, so it compiled locally while CI never saw it. Same class as
# commit e2e7956 "remove accidental duplicate ca config file".

# Build artifacts that kept landing in the working tree

# macOS duplicates. Finder/iCloud creates "file 2.ext" copies; 42 of them had
# accumulated inside .next and one ("SECURITY 2.md") at the repo root, where it
# silently shadowed the real file in searches.
* [0-9].*

# Agent scratch + session research (keep locally, never commit)
.hermes/
findings.md
